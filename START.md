# 🚀 تشغيل التطبيق على Localhost

## الطريقة السريعة

### استخدام السكريبت:
```bash
./start.sh
```

أو:

```bash
npm start
```

## الطريقة اليدوية

```bash
npm run dev
```

## الوصول للتطبيق

بعد التشغيل، افتح المتصفح على:
- **http://localhost:5173**

## ملاحظات

- السكريبت `start.sh` يقوم تلقائياً بـ:
  - إيقاف أي خادم قديم يعمل على المنفذ 5173
  - التحقق من تثبيت الحزم
  - تشغيل الخادم التطويري

- لإيقاف الخادم: اضغط `Ctrl+C` في نافذة Terminal

