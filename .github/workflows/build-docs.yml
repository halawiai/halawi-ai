name: Build documentation

# Disabled: This workflow was for Hugging Face documentation builds
# To re-enable, create docs/source/_toctree.yml and configure documentation
on:
  workflow_dispatch:  # Only allow manual runs
  # push:
  #   branches:
  #     - main
  #     - v*-release

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Documentation build disabled
        run: |
          echo "Documentation build is disabled for Halawi AI."
          echo "To enable, create docs/source/_toctree.yml and configure the build."
          exit 0
  # build:
  #   uses: huggingface/doc-builder/.github/workflows/build_main_documentation.yml@main
  #   with:
  #     commit_sha: ${{ github.sha }}
  #     package: chat-ui
  #     additional_args: --not_python_module
  #   secrets:
  #     token: ${{ secrets.HUGGINGFACE_PUSH }}
  #     hf_token: ${{ secrets.HF_DOC_BUILD_PUSH }}
